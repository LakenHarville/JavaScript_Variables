console.log("Hello World!\n==========\n");
console.log(
  "Follow the steps in the README.md file to complete the exercises:\n==========\n"
);

// Exercise 1

const firstName = "Laken";
let lastName = "Harville";
var age = 26;

// Exercise 2

let fullName = firstName + " " + lastName;
console.log(fullName);

// Exercise 3

let city = "Tuscaloosa";
let passTime = "weightlifting";
let myStory = `Hello. I am ${fullName}. I live in ${city}. I partake in ${passTime} and coding.`;
console.log(myStory);

// Exercise 4

let price = 19.99;
let isOnSale = false;
let salePercentage = 15;
let stock = 0;
let inStock = false;
let selectedSize = "M";
let itemOnSale = `Whatever is priced at ${price}. There is ${stock} in stock. It is ${selectedSize} sized only.`
console.log(itemOnSale);

// Exercise 5

let title = "Name of the Wind";
let author = "Patrick Rothfuss";
let pageCount = 722;
let bookMark = 456;
let hasRead = true;
let book = `${title} by ${author} is ${pageCount} pages long. I haven't finished it. I'm currently bookmarked at page ${bookMark}.`
console.log(book);

// Exercise 6

let language = "Javascript";
let createdYear = 1995;
let isCaseSensitive = true;
let languageRead = `${language} was created in ${createdYear}.`
console.log(languageRead);

// The exercises aren't exactly in order because I followed the web page instructions before I followed the instructions on the README.md.